# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only preview of the hero video before it's uploaded to iGEM Video
# Universe — never commit this; per README.md, video must be served from
# iGEM infrastructure, not bundled in the repo.
/public/temp-preview/
